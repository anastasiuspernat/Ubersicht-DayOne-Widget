# Ubersicht-DayOne-Widget
The widget displays random photos from DayOne's iCloud-based diary on a Mac desktop using Übersicht.

Needs exiftool. Currently the path to the journal must be set manually.
